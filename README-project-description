## Project description

The were some issues with the website some of the issues were
-Duplicates in the CSS code
-Replacing the divs with more specific tags
-Adding alt text to the images
-Making the HTML code align with best practices